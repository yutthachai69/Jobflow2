// scripts/demo-work-order-numbers.js
// สาธิตรูปแบบเลขที่งานใหม่ 8vxgpup####

const WORK_ORDER_PREFIX = '8vxgpup'

console.log('📋 สาธิตรูปแบบเลขที่งานใหม่: 8vxgpup####\n')
console.log('=' .repeat(50))
console.log('รูปแบบ: [prefix][sequence 4 หลัก]')
console.log('Prefix: 8vxgpup (คงที่)')
console.log('Sequence: 0001, 0002, 0003... (รันต่อเนื่องไม่รีเซ็ต)\n')
console.log('=' .repeat(50))
console.log('\n📝 ตัวอย่างเลขที่งาน:\n')

// สร้างตัวอย่าง 20 ใบงาน
for (let i = 1; i <= 20; i++) {
  const sequence = String(i).padStart(4, '0')
  const workOrderNumber = `${WORK_ORDER_PREFIX}${sequence}`
  
  // สร้างวันที่สมมติ (ไม่เกี่ยวกับเลขที่งานแล้ว)
  const date = new Date()
  date.setDate(date.getDate() - (20 - i)) // กระจายไป 20 วัน
  
  const dateStr = date.toLocaleDateString('th-TH', {
    year: 'numeric',
    month: '2-digit',
    day: '2-digit'
  })
  
  console.log(`  ${workOrderNumber.padEnd(15)} | วันที่: ${dateStr} | (ลำดับที่ ${i})`)
}

console.log('\n' + '=' .repeat(50))
console.log('\n✅ สรุป:')
console.log('  - เลขที่งานรันต่อเนื่อง: 8vxgpup0001 → 8vxgpup0002 → 8vxgpup0003...')
console.log('  - ไม่รีเซ็ตตามวัน/เดือน/ปี')
console.log('  - รันต่อเนื่องไปเรื่อยๆ จนถึง 8vxgpup9999 (10,000 ใบงาน)')
console.log('  - หลังจาก 9999 จะเป็น 8vxgpup10000 (5 หลัก)')
console.log('\n📌 เปรียบเทียบกับรูปแบบเก่า:')
console.log('  ❌ เก่า: 6901250001, 6901250002, 6901260001 (รีเซ็ตทุกวัน)')
console.log('  ✅ ใหม่: 8vxgpup0001, 8vxgpup0002, 8vxgpup0003 (รันต่อเนื่อง)')
console.log('\n')
